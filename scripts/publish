#!/bin/bash
DIR=`dirname $0`
SCRIPT=`basename $0 | tr abcdefghijklmnopqrstuvwxyz ABCDEFGHIJKLMNOPQRSTUVWXYZ`
APP=$DIR/..
set -e

echo $SCRIPT: BEGIN `date`

npm version -ws --include-workspace-root minor

### Publish EN
pushd dpd/en > /dev/null
echo $SCRIPT directory `pwd`
echo $SCRIPT: publishing `cat package.json | grep '"name"'
npm publish
popd > /dev/null

### Publish main
echo $SCRIPT directory `pwd`

echo $SCRIPT: updating ms-dpd dependencies
npm install --save @sc-voice/ms-dpd-en@latest
VER=`npm ls | head -1 | sed 's/.*@\([0-9.]*\).*/\1/'`

echo $SCRIPT: commit $VER
git commit -am "$VER"

echo $SCRIPT $VER

echo $SCRIPT: publish `cat package.json | grep '"name"'
npm publish
npm ls

echo $SCRIPT: END `date`
